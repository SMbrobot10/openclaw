[build]
dockerfilePath = "Dockerfile.railway"

[deploy]
startCommand = "mkdir -p /data/.openclaw /data/workspace && node openclaw.mjs gateway --allow-unconfigured --port 8080 --bind lan"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
